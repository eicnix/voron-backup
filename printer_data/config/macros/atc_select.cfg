[gcode_macro _ATC_SELECT]
gcode:
    {% set bed = params.BED|default(0)|float %}
    {% set name = params.PROFILE|default("") %}
    {% if not name %}
        {% set name = "hot" if bed >= 95 else ("warm" if bed >= 75 else "cold") %}
    {% endif %}
    ATC_PROFILE LOAD={name}
